# Upstream Maida contract snapshot

These files are exact test snapshots of the authoritative contract in
`maida/contracts/`. Do not edit them independently. Update Python first, copy
the changed snapshot, and run the workspace cross-repository sync checker.
